# barcode-app
یک برنامه برای ذخیره پرونده ها و تولید بارکد برای آنها با nodejs


npm install express jsbarcode xmldom lowdb@1 systray bootstrap

npm install -g @yao-pkg/pkg
npm config set allow-scripts=esbuild --location=user


pkg server.js --targets node24-win-x64 --output dist/barcode-app.exe
